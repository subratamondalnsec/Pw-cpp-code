#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cout<<"check the three digit numder :";
    cin>>n;
    if(n>100&&n<999)
    cout<<"yes its three digit numder";
    else
     cout<<"no its not a three digit numder";
}